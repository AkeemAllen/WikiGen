// place files you want to import through the `$lib` alias in this folder.
import NavButton from "./NavButton.svelte";
import WikiSelectMenu from "./WikiSelectMenu.svelte";

export { NavButton, WikiSelectMenu };
